# Sample Data

Local Tamil author samples and source news examples live under this directory.

The following directories are local-only and ignored by Git:

- `sample_data/raw/`
- `sample_data/extracted/`

Do not commit API keys, raw article archives, or extracted article text.
